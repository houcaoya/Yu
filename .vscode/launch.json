{
    "configurations": [
        {


            "name": "STM32 Debug (DAP)",

            "type": "cortex-debug",

            "request": "launch",

            "servertype": "openocd",

            "serverpath": "D:/OpenOCD-20250710-0.12.0/bin/openocd.exe", // 修改成自己的

            "cwd": "${workspaceFolder}",

            "executable": "build/Debug/Yu.elf", // 修改成自己的

            "configFiles": [

                "${workspaceFolder}/openocd.cfg"

            ],

            "runToEntryPoint": "main",
            "liveWatch": {
            "enabled": true,
            "samplesPerSecond": 20,
            // "graph": ["variable1", "variable2"] // 可选：可以图形化显示变量变化的变量名
            }
        }
    ],

}